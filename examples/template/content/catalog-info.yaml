apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ${{ values.name | dump }}
  image: ${{ values.image_name | dump }}
spec:
  type: service
  owner: user:guest
  lifecycle: experimental
